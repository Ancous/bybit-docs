- [Информация](#информация)
- [Конечная точка](#конечная-точка)
- [Примеры запроса](#примеры-запроса)
- [Параметры запроса](#параметры-запроса)
- [Пример ответа](#пример-ответа)
- [Параметры ответа](#параметры-ответа)

<a id="информация"></a>

## Информация

Запрос списка обменных сущностей

>Информация::  
>
>Эта конечная точка используется для пользователей с `kyc`=`KOR` (Корея). При выводе средств вам нужно заполнить
> идентификатор сущности.

<a id="конечная-точка"></a>

## Конечная точка

`/v5/asset/withdraw/vasp/list`

<a id="примеры-запроса"></a>

## Примеры запроса

- HTTP

  ```http
  GET /v5/asset/withdraw/vasp/list HTTP/1.1
  Host: api-testnet.bybit.com
  X-BAPI-API-KEY: "<api_key от биржи bybit>"
  X-BAPI-SIGN: <подпись>
  X-BAPI-TIMESTAMP: 1715067106163
  X-BAPI-RECV-WINDOW: 5000
  ```

- собственная реализация

  ```python
  import time
  import hmac
  import hashlib
  import requests

  from urllib.parse import urlencode

  base_url = "https://api-testnet.bybit.com"
  end_point = "/v5/asset/withdraw/vasp/list"
  complete_request = base_url + end_point

  api_key = "<api_key от биржи bybit>"
  secret_key = "<secret_key от биржи bybit>"
  time_stamp = str(int(time.time() * 1000))
  recv_window = "5000"

  data = {}

  param_str = time_stamp + api_key + recv_window + urlencode(data)
  
  signature = hmac.new(
      key=secret_key.encode("utf-8"),
      msg=param_str.encode("utf-8"),
      digestmod=hashlib.sha256
  ).hexdigest()
  
  headers = {
    "X-BAPI-API-KEY": api_key,
    "X-BAPI-SIGN": signature,
    "X-BAPI-TIMESTAMP": time_stamp,
    "X-BAPI-RECV-WINDOW": recv_window,
  }

  response = requests.get(url=complete_request, headers=headers, params=data, timeout=10)

  print(response.json())
  ```

<a id="параметры-запроса"></a>

## Параметры запроса

Отсутствуют

<a id="пример-ответа"></a>

## Пример ответа

```json
{
    "retCode": 0,
    "retMsg": "success",
    "result": {
        "vasp": [
            ...
            {
                "vaspEntityId": "codevasp",
                "vaspName": "codevasp"
            },
            {
                "vaspEntityId": "hanbitco",
                "vaspName": "hanbitco"
            },
            {
                "vaspEntityId": "others",
                "vaspName": "Others (including Upbit)"
            },
            ...
        ]
    },
    "retExtInfo": {},
    "time": 1715067106537
}
```

<a id="параметры-ответа"></a>

## Параметры ответа

| Параметр     | Тип    | Комментарии                                                                                                                                          |
|--------------|--------|------------------------------------------------------------------------------------------------------------------------------------------------------|
| vasp         | array  | ***Информация об объекте обмена.***                                                                                                                  |
| vaspEntityId | string | ***Идентификатор платформы получателя.***<br><br>- При переводе на Upbit или другие биржи, не входящие в список, используйте `vaspEntityId`=`others` |
| vaspName     | string | ***Название платформы получателя.***                                                                                                                 |
