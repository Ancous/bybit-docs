- [Информация](#информация)
- [Конечная точка](#конечная-точка)
- [Примеры запроса](#примеры-запроса)
- [Параметры запроса](#параметры-запроса)
- [Пример ответа](#пример-ответа)
- [Параметры ответа](#параметры-ответа)

<a id="информация"></a>

## Информация

Запрос на погашения займа

> Зона применения:
>
> `linear` - контракт (расчет в USDT, USDC, ...)
>
> - `Perpetual` - контракт без даты экспирации
> - `Futures` - контракт с датой экспирации
<!-- -->
> Информация:
>
> - Изменения с 2026 года
>
>   - BYUSDT для погашения: Начиная с 17 марта 2026 года (постепенное внедрение, доступно с 24 марта 2026 года)
>     для погашения можно будет использовать BYUSDT.
>
> - Ограничения по MNT
>
>   - Использование MNT для погашения временно приостановлено. Погашение обязательств в MNT через функцию конвертации
>     (convert-repay) не поддерживается.
>   - Однако вы по-прежнему можете использовать
>     [Ручное погашение займа без конвертации активов](<Ручное погашение займа без конвертации активов.md>),
>     чтобы погасить MNT из текущего баланса.
>
> - Обновление механизма ручного погашения UTA Loan (с 10 февраля 2026)
>   Начиная с 10 февраля 2026 года, 08:00 UTC, в механизм ручного погашения UTA Loan вносятся следующие изменения:
>
>   - Комиссия за конвертацию валюты при погашении будет рассчитываться с использованием наибольшей ставки комиссии
>     (из двух: ставка для залогового актива или для актива-долга).
>   - Вводится лимит на конвертацию валюты в одной транзакции — 300 000 USD (общая сумма конвертируемых средств не
>     должна превышать эквивалент 300 000 USD). Это сделано для повышения стабильности и усиления контроля за рисками.
>   - Подробнее см. в объявлении: [UTA Loan manual repayment update](https://announcements.bybit.com/en/article/uta-loan-manual-repayment-update-bltbef3f1ad72a8295d/).

<a id="конечная-точка"></a>

## Конечная точка

`/v5/account/quick-repayment`

<a id="примеры-запроса"></a>

## Примеры запроса

- HTTP

  ```http
  POST /v5/account/quick-repayment HTTP/1.1
  Host: api-testnet.bybit.com
  X-BAPI-API-KEY: "<api_key от биржи bybit>"
  X-BAPI-SIGN: <подпись>
  X-BAPI-TIMESTAMP: 1701848610019
  X-BAPI-RECV-WINDOW: 5000
  Content-Type: application/json
  Content-Length: 22

  {
      "coin": "USDT"
  }
  ```

- собственная реализация

  ```python
  import time
  import hmac
  import hashlib
  import json
  import requests

  base_url = "https://api-testnet.bybit.com"
  end_point = "/v5/account/quick-repayment"
  complete_request = base_url + end_point

  api_key = "<api_key от биржи bybit>"
  secret_key = "<secret_key от биржи bybit>"
  time_stamp = str(int(time.time() * 1000))
  recv_window = "5000"

  data = {
      "coin": "USDT"
  }

  param_str = time_stamp + api_key + recv_window + json.dumps(data)
  
  signature = hmac.new(
      key=secret_key.encode("utf-8"),
      msg=param_str.encode("utf-8"),
      digestmod=hashlib.sha256
  ).hexdigest()
  
  headers = {
    "X-BAPI-API-KEY": api_key,
    "X-BAPI-SIGN": signature,
    "X-BAPI-TIMESTAMP": time_stamp,
    "X-BAPI-RECV-WINDOW": recv_window,
  }

  response = requests.post(url=complete_request, headers=headers, json=data, timeout=10)

  print(response.json())
  ```

<a id="параметры-запроса"></a>

## Параметры запроса

| Параметр | Обязательный  | Тип    | Комментарии                                                                                                                                                                            | По умолчанию |
|----------|---------------|--------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------|
| coin     | нет           | string | ***Монета с обязательством.***<br><br>- Только заглавными буквами<br>- Поле заполнено: погасить обязательство именно этой монеты.<br>- Поле пустое: погасить обязательство всех монет. | -            |

<a id="пример-ответа"></a>

## Пример ответа

```json
{
    "retCode": 0,
    "retMsg": "SUCCESS",
    "result": {
        "list": [
            {
                "coin": "BTC",
                "repaymentQty": "0.10549670"
            },
            {
                "coin": "ETH",
                "repaymentQty": "2.27768114"
            }
        ]
    },
    "retExtInfo": {},
    "time": 1701848610941
}
```

<a id="параметры-ответа"></a>

## Параметры ответа

| Параметр     | Тип    | Комментарии                                                                                                                                                                           |
|--------------|--------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| list         | array  | ***Массив объектов.***                                                                                                                                                                |
| coin         | string | ***Монета, используемая для погашения.***<br><br>- Порядок валют, используемых для погашения обязательств, основан на порядке ликвидации с [этой конечной точки SPOT MARGIN TRADE](#) |
| repaymentQty | string | ***Сумма погашения.***                                                                                                                                                                |
